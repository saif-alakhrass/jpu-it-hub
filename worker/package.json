{
  "name": "jpu-it-hub-storage-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "wrangler deploy --dry-run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.5.3",
    "vitest": "^4.1.10",
    "wrangler": "^3.80.0"
  }
}
